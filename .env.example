# Copy to .env for local, non-secret vars only. .env is git-ignored.
# The EIA API key does NOT belong here — it lives in the `energy_transition`
# Databricks secret scope. Read it in notebooks via dbutils.secrets.get.

# Optional: override the default bundle target for local CLI runs.
# DATABRICKS_BUNDLE_ENV=dev
